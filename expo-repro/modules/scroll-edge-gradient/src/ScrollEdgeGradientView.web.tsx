import { ScrollEdgeGradientViewProps } from './ScrollEdgeGradient.types';

// ScrollEdgeGradientView is not available on the web platform.
export default function ScrollEdgeGradientView(_props: ScrollEdgeGradientViewProps) {
  return null;
}
